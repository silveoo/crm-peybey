import {describe,expect,it} from 'vitest';import {bookingSchema} from '../booking.schema';it('validates booking form',()=>{expect(bookingSchema.safeParse({tableId:'t',bookingDate:'2026-07-15',startTime:'10:00',endTime:'11:00',guestName:'Иван',phone:'',guestCount:2,comment:'',status:'confirmed'}).success).toBe(true);expect(bookingSchema.safeParse({tableId:'t',bookingDate:'2026-07-15',startTime:'11:00',endTime:'10:00',guestName:'',guestCount:0,status:'confirmed'}).success).toBe(false)});
