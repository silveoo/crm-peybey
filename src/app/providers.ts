import {SqliteBookingRepository} from '../repositories/sqlite/SqliteBookingRepository';import {SqliteTableRepository} from '../repositories/sqlite/SqliteTableRepository';import {SqliteSettingsRepository} from '../repositories/sqlite/SqliteSettingsRepository';import {BookingService} from '../services/BookingService';import {TableService} from '../services/TableService';export const tableRepo=new SqliteTableRepository();export const bookingRepo=new SqliteBookingRepository();export const settingsRepo=new SqliteSettingsRepository();export const tableService=new TableService(tableRepo);export const bookingService=new BookingService(bookingRepo);
